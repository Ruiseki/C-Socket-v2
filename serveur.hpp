#ifndef SERVEUR_HPP
#define SERVEUR_HPP

#include <WinSock2.h>
#include <string>
#include <thread>
#include <fstream>

class Serveur
{
    public:
        Serveur();
        ~Serveur();
    private:
};

#endif